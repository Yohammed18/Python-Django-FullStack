# import module as mm
from module import func_in_module 


# In simpler terms, the __pycache__ directory in Python is like a storage area where the Python interpreter keeps a version of your code that it can quickly use when you run your program again. Instead of starting from scratch every time you run your Python script, the interpreter can use this stored version, called bytecode, which is a more efficient form of your code. 
func_in_module()